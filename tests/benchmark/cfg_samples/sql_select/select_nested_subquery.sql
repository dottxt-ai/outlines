-- SQL statement generated by ChatGPT
SELECT DISTINCT a.name, (SELECT AVG(b.salary) FROM employees b WHERE b.department_id = a.department_id) AS avg_salary FROM departments a WHERE a.location = 'New York' ORDER BY a.name, avg_salary DESC LIMIT 5 OFFSET 10
