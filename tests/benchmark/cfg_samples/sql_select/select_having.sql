-- SQL statement generated by ChatGPT
SELECT a.department_id, COUNT(*) AS num_employees, AVG(b.salary) FROM employees a INNER JOIN salaries b ON a.id = b.employee_id GROUP BY a.department_id HAVING AVG(b.salary) > 50000
